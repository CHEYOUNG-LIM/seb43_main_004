version: 0.2

phases:
  install:
    runtime-versions:
      java: corretto11
  build:
    commands:
      - echo Build Starting on `date`
      - cd DeployServer
      - chmod +x ./gradlew
      - ./gradlew build
  post_build:
    commands:
      - echo $(basename ./server/wrieating/build/libs/*.jar)
artifacts:
  files:
    - server/wrieating/build/libs/*.jar
    - server/wrieating/scripts/**
    - server/wrieating/appspec.yml
  discard-paths: yes